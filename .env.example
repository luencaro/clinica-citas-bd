# ============================================================================
# Environment Variables - Medical Appointments Management System
# ============================================================================
# This is an example file. Copy to .env and update with your values.
# DO NOT commit .env file to version control!

# Database Configuration
# ----------------------------------------------------------------------------
POSTGRES_USER=clinica_admin
POSTGRES_PASSWORD=clinica_2025_secure
POSTGRES_DB=clinica_citas
POSTGRES_PORT=5432
DB_HOST=db
DB_PORT=5432
DB_NAME=clinica_citas
DB_USER=clinica_admin
DB_PASSWORD=clinica_2025_secure

# Database Connection Pool
# ----------------------------------------------------------------------------
DB_MIN_CONN=1
DB_MAX_CONN=10

# Database Retry Configuration
# ----------------------------------------------------------------------------
DB_MAX_RETRIES=5
DB_RETRY_DELAY=5

# Application Configuration
# ----------------------------------------------------------------------------
APP_ENV=development
APP_PORT=5000
LOG_LEVEL=INFO

# Security (change in production!)
# ----------------------------------------------------------------------------
SECRET_KEY=change-this-in-production-use-random-string
JWT_SECRET=change-this-in-production-use-random-string
JWT_EXPIRATION=3600

# Email Configuration (for notifications - optional)
# ----------------------------------------------------------------------------
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@clinica.com

# Timezone
# ----------------------------------------------------------------------------
TZ=America/Bogota
